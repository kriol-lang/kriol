inpristan "stdio.h"

num soma(num n) {
    num acc = 0;

    num i;
    pa i = 0 ; i < n ; i = i + 1 {
        acc = acc + i;
    }

    divolvi acc;
}

vaziu inisiu() {
    mostra("Soma ti 10 é igual a %g\n", soma(10));
}
