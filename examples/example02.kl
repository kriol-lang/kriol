fn soma(num n) num {
    num acc = 0;

    nter i;
    pa i = 1 ; i < n ; i = i + 1 {
        acc = acc + i;
    }

    divolvi acc;
}

fn inisiu() {
    mostran("Soma di 1 ti 10 é igual a {}", soma(10));
}
