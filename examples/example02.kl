fn soma(num n) num {
    num acc = 0;

    nter i;
    pa i = 0 ; i < n ; i = i + 1 {
        acc = acc + i;
    }

    divolvi acc;
}

fn inisiu() {
    mostra("Soma ti 10 é igual a %g\n", soma(10));
}
