fn soma(num n) num {
    num acc = 0;

    dipoz nter i;
    pa i = 1 ; i < n ; i += 1 {
        acc = acc + i;
    }

    divolvi acc;
}

fn inisiu() {
    num n = 10;
    num resultadu = soma(n);
    mostran(f"Soma di 1 ti {n} é igual a {resultadu}.");
}
